package pointlike

version v1r4p1

# $Header$

use STpolicy *
use map_tools *
use embed_python *

apply_pattern package_include
private
apply_pattern ST_library option=-no_share

macro source "*.cxx" WIN32 "*.cxx *.h *.py"

# fitting application, 
application pointfit -s=pointfit $(source) -s=../python pointfit_setup.py


#souce-finding application
application finder -s=finder $(source) -s=../python finder_setup.py
